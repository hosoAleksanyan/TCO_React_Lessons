function CustomButton({ title }) {
    // console.log(title);
    return (
        <button style={{ background: 'lightgrey' }}>
            {title}
        </button>
    );
}

export default CustomButton;